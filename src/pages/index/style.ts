import { css } from 'styled-components'

export default css`
  > .header {
    position: relative;
    width: 100%;
    height: 20rem;
    background: #ffd632;
    > .title {
      display: flex;
      justify-content: center;
      position: absolute;
      bottom: -3rem;
      width: 100%;
      color: #1a252f;
      font-size: 10rem;
      font-weight: bold;
      font-family: 'Pacifico', cursive;
    }
  }
`
